<div class="row">
  <%= form_with url: api_v1_user_path(@user), method: :put do |f| %>
  <div class="card">
    <div class="card-body">
      <div class="row">
        <div class="col-sm-4">
          <div class="avatar-bg">
            <%= image_tag @user.avatar if @user.avatar.attached?%>
          </div>
        </div>
        <div class="col-sm-8 margin-top">
          <h5><b><%= @user.email %></b></h5>
          <% if current_user.supervisor.present? %>
            <h6>Du bist Supervisor</h6>
          <% end %>
          <% if @user_team_ids.present? %>
            <%= f.select('switch-team', @user_team_ids.each {|a| a}) %>
          <%else %>
            <p class="red-text">Du musst noch einem Team beitreten </br> oder ein neues Teamerstellen</p>
          <%end %>
        </div>
      </div>
      <div class="col-sm-12 margin-top">
          <div class="margin-bottom">
            <h6 class="margin-bottom purple-text" data-toggle="collapse" href="#collapseExample" role="button" aria-expanded="false" aria-controls="collapseExample">
              Neues Team
            </h6>
            <div class="border padding collapse " id="collapseExample">
              <div>
                <div>
                  <p>Bist du dir sicher? <%= f.check_box :team_id %></p>
                  <p><%= f.text_field 'team-name', placeholder: 'Team-Name' %></p>

                </div>
              </div>
            </div>
          </div>
          <%if !current_user.supervisor.present? && Supervisor.find_by(team_id: current_user.team_id).nil?%>
            <div class="margin-bottom">
              <p>Erweiterte Rechte? (Desks, Benutzer usw. verwalten) <%= f.check_box :supervisor %></p>
            </div>
          <%end %>
          <div class="margin-bottom">
            <p>Profilbild: <%= f.file_field :avatar %></p>
          </div>
          <div class="margin-top">
            <%= f.submit 'speichern', :class => 'btn btn-primary' %>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
